{
  "name": "nebeltisch",
  "scripts": {
    "dev": "bun run build:client && bun run --hot src/index.ts",
    "start": "bun run src/index.ts",
    "test": "bun test",
    "build:client": "bun build public/js/gm.ts public/js/player.ts --outdir public/dist --splitting --target browser"
  },
  "devDependencies": {
    "@types/bun": "^1.3.13"
  }
}
